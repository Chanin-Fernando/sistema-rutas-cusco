# Funciones auxiliares para dibujar el mapa (se mantienen dentro de app.py por simplicidad,
# pero las extraemos para que el código sea más limpio. La App original tenía el método _dibujar_mapa
# dentro de AppCusco. Para no modificar funcionalidad, lo dejamos en app.py.
# Este archivo se puede usar en el futuro para refactorizar.